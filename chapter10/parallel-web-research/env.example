# 复制为 .env 后按需填写。全部留空也能跑通（走离线关键词判断，结果可复现）。

# —— 可选：让子 Agent 用真实 LLM 判断"网页是否命中答案" ——
# 首选 OPENAI_API_KEY（直连 OpenAI），默认当前便宜旗舰 gpt-5.6-luna。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
# 自定义 OpenAI 兼容网关时填写，例如 Moonshot / 火山方舟 ARK：
# OPENAI_BASE_URL=https://api.moonshot.cn/v1
# OPENAI_MODEL=kimi-k3

# 通用回退：若未设置 OPENAI_API_KEY，则自动改用 OPENROUTER_API_KEY 走 OpenRouter，
# 并把模型名映射到其命名空间（gpt-5.6-luna -> openai/gpt-5.6-luna）。
# OPENROUTER_API_KEY=

# 注意：真实 LLM 判断需同时满足 USE_LLM=1 且配置了上述任一 key，否则自动回退离线关键词判断。
# 设为 1/true 才启用真实 LLM 判断（默认留空 = 离线关键词判断，可复现）：
USE_LLM=
